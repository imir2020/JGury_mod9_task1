spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/springdata
  jpa:
    properties.hibernate:
      show_sql: true
      format_sql: true
      hbm2ddl.auto: validate
    open-in-view: false
